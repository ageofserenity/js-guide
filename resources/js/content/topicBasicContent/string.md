# Section 3.3 Strings — Skipped Lessons

## 3.3.13 common methods — SKIPPED

**Reason:** Most string method coverage lives in Section 5 (Method/Snippet
Reference), which already has the 18-piece lesson format applied to each
individual method (toUpperCase, toLowerCase, trim, includes, slice, split,
replace, indexOf, startsWith, endsWith, etc.).

Creating a "common methods" lesson in 3.3 would duplicate that work.

**If revisited later, options:**
- Turn 3.3.13 into a short OVERVIEW lesson (~5 sentences per method) with
  links pointing into Section 5 for the full treatment.
- Remove 3.3.13 from data.js entirely and update the section flow.
- Keep it as-is (empty) and rely on Section 5 as the canonical source.